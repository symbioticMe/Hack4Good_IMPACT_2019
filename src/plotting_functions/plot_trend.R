plot_estimate <- function(df, 
                          final_month, 
                          time_window = 6,
                          conf_interval = .95,
                          price_col = 'SMEB', 
                          geogr_level = 'q_district',
                          geogr_columns = c('region','q_distr','q_sbd')){
  
}